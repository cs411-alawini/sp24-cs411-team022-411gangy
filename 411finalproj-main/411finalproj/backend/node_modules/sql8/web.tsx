import { SetStore, GetStore, action } from 'mobz'
import { TablesT, Web, Serve } from './serve'
import { FC } from 'react'
import { ipcRenderer } from 'electron'

export class electron<
    Tables extends TablesT,
    Joins extends Record<keyof Tables, keyof Tables>,
    > extends Web<Tables, Joins>{
    constructor(
        JOINS: { [T in keyof Tables]: string[] },
        get: GetStore<Web<Tables, Joins>>,
        set: SetStore<Web<Tables, Joins>>
    ) {
        super(JOINS)
    }
}

// axios() { }
    // socketIo() {
    //     // const baseRes: uiIpcBase.BaseSqlRes = action(({ req, res }) => {
    //     //     const { catchIo, success, edit, table } = res
    //     //     if (catchIo) {
    //     //         return catchIo
    //     //     } else {
    //     //         const t = self()[table]
    //     //         if (edit === 'del') {
    //     //             for (const v of success) delete t[v.id]
    //     //         } else {
    //     //             success.forEach((v: any) => t[v.id] = v)
    //     //         }
    //     //         set(({ [table]: t }))
    //     //     }
    //     // })
    // }